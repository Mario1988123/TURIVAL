import { AppLayout } from '@/components/layout/app-sidebar'

export default function ProveedoresLayout({ children }: { children: React.ReactNode }) {
  return <AppLayout title="Proveedores">{children}</AppLayout>
}
