import { AppLayout } from '@/components/layout/app-sidebar'

export default function TrazabilidadLayout({ children }: { children: React.ReactNode }) {
  return <AppLayout title="Trazabilidad">{children}</AppLayout>
}
