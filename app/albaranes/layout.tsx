import { AppLayout } from '@/components/layout/app-sidebar'

export default function AlbaranesLayout({ children }: { children: React.ReactNode }) {
  return <AppLayout title="Albaranes">{children}</AppLayout>
}
